# Checkpoint Journal — 臥龍傳 Web 复刻

> 写给下一个对话：本文档记录项目稳定事实、当前架构、约定、坑点与主线状态。
> 日期：2026-08-28 ｜ 服务器：`cd E:/Dragon/web-port && python tools/webserver.py 8321` → <http://127.0.0.1:8321/>

---

## 一、项目概览（稳定事实）

- **目标**：Web 复刻 DOS 经典《臥龍傳》（1995）。原版二进制在 `E:/Dragon/Dragon/`：KI.EXE（主程序 66KB）、MMAP.MAP/.MDL/.MCH（地图/tile/旗帜）、GAMEPAL.BRG（4季×16色 6-bit）、sinario.dat（剧本）、SAVE.DAT。
- **Web 端**：`E:/Dragon/web-port/web/`，原生 JS + Canvas，无构建。`data.json` = 20 剧本解析结果（城池含 type 0-5）。
- **原版显示模式**：VGA mode 0x12（640×480 16 色，`mov ax,0x12; int 0x10` @KI.EXE 0xEB6C）。
- **代码索引与快速查找**：项目已配置并运行 CodeGraph 索引插件（SQLite FTS5 + WAL，112 文件 / 799 节点 / 1815 边），支持 `codegraph_search` / `codegraph_callers` / `codegraph_impact` 毫秒级符号与调用链定位。
- **逆向工具**：`web-port/tools/disasm.py`（capstone：`func(addr,len)`、`callers(addr)`）；`render_map.py` 提供 `load_palette(bank)`/`load_tiles()`（**返回 list[256] 非 dict**）。
- **验证方式**：Playwright 走真实菜单流程（开场→YES→章节→势力→确定）。**坑**：`frame` 主循环在 `await startMenu.show()` 之后才启动；移除 startv DOM 会导致 `__aiTick` 等调试句柄未定义。
- **时钟**：`game/clock.js`，1 tick=1 时刻（0-23），24 tick=1 天；速度档 [0,512,256,96] ms/tick（中速≈6.1 秒/天）；换月调月度结算。
- **军师菜单交互规范与全局约束（2026 最新用户定稿）**：
  1. **羽扇图标一级乒乓开关**：羽扇图标为全局乒乓开关，隐藏时强制关闭所有子孙弹窗与选中状态并恢复计时；能关闭军师菜单的**只有再次点击羽扇图标**；
  2. **单选互斥与绝对锁定**：8 项子菜单（進言、人事、財政、編成、軍團、據點、武將、勢力）严格单选。任意子项被打开时，大地图全局锁定（不可拖拽平移，地图交互拾取全部失效），其余 7 项子菜单不可点击且无悬停高亮；
  3. **严格右键回退与无关闭按钮**：全游戏无任何 UI 关闭按钮，所有弹窗与二级菜单只能通过鼠标右键回退关闭。第 1 次右键关闭子弹窗并取消子项选中状态，恢复未选中并立即开始计时（`clock.hold=false`），菜单条保持展开；再次右键关闭军师菜单条；
  4. **地图左键纯粹性**：除据点中心与行走军团外，点击地图空白/山川/河流没有任何功能，不关闭弹窗/菜单。

---

## 二、近期完成专题汇总（2026-08-28）

### 1. 势力信息面板（右侧君主/资源面板）1:1 还原

- **黑底底框容器**：在信赖度下方绘制 `192×72` 黑底方框（`p.x + 8, p.y + 112`）；
- **红底黑色剪影图标**：尺寸 `22×14px`，红底 `#d00000`，由上至下 4 行依次为：
  1. 資金：天平图标 (`ico_money.png`)
  2. 騎兵：马头图标 (`ico_cavalry.png`)
  3. 弓兵：弓箭手图标 (`ico_archer.png`)
  4. 步兵：步兵图标 (`ico_infantry.png`)
- **排版与对齐**：左侧「資金」「預備兵」米黄字（`CREAM`），图标居中（`p.x + 95`），数字白色右对齐（`p.x + 196`）；
- **数据源与刷新机制**：直接动态绑定 `me.gold`、`me.reserve_cav`、`me.reserve_arc`、`me.reserve_inf`，数据在每月 1 日财政结算时自动刷新联动。

### 2. 军师菜单「編成」功能与部队编制系统

- **空闲武将选择列表 (`showFormation`)**：480×352 Canvas 金框云纹列表（`scrollbar: "right"`），过滤显示身分为 `－－－` 的武将，底部 NPC 提示；
- **部队编成面板 (`showFormationDialog`)**：15×12 tiles（240×192）金框云纹弹窗。
  - 顶部武将头像、將軍名、總兵力实时合计、士氣值 200；
  - 左下 6 队（主將、前鋒、左翼、右翼、左備、右備）带绿底按钮，点击循环切换 `騎兵 → 步兵 → 弓兵`；
  - 兵力分配守恒算法：每队上限 1000，优先均分预备兵池，扣除后剩余保留在预备兵；
- **编成警告与武将发言弹窗 (`KI.EXE` 0x6F14 / 0x6F32 逆向)**：
  - 主将或前锋兵力为 0 时，弹出军师头像警告（`TALK.DAT` 62：`大將的部隊的兵員不足啊。`）；
  - 编成成功弹出武将发言（`446 + talk_idx`）；
  - **弹窗加宽 30%（20 tiles = 320px）**，并实装避头尾标点自适应排版，台词舒展排开无单独句号掉行；
  - 确定生成军团驻守于首都，视角自动居中并选中首都。

### 3. 军师菜单「據點」与驻军军团情报

- **据点二级菜单 (`baseMenu`)**：7×4 tiles 黑底金框云纹菜单，包含「首都確認」与「據點一覽」；
- **首都確認**：镜头平滑居中至首都并显示据点卡片与正方形选中框；
- **據點一覽**：我方据点列表（生產力、上昇率、防災、城兵、內政官；上升率 $\le 0$ 标红；右侧 18px 滚动条）；
- **驻军据点二级菜单与軍團情報列表**：驻军据点弹出 `據點 / 軍團` 7×3 tiles 垂直菜单；军团情报列表按 10 行原版规格排版（不足 10 行以虚线 `－－－－` 补齐）。

### 4. 军团行走平滑逐帧动画

- **时钟流逝进度 (`clock.dayProgress`)**：按每日时刻与 tick 生成 $t \in [0.0, 1.0)$，暂停时锁定，调速时等比缩放；
- **逻辑与渲染分离**：军团维护 `prevX, prevY` 起点与 `x, y` 目标点；
- **逐帧插值与道路中线吸附 (`mapview.js`)**：格点与道路偏移量线性插值（lerp），凸标精确贴合道路中心线，朝向角度随动，行军虚线自当前屏幕像素点引出并随推进收缩，进出城平滑过渡，屏幕拾取与悬停精准套选。

### 5. 军师菜单「財政」弹窗与数字键盘输入系统 (KI.EXE 0x678D / 0x7C6E / 0x5358 1:1 逆向复刻与体验优化)

- **财政主弹窗居中 (`financeDialog`)**：21×10 tiles（336×160）金框云纹弹窗；上方配有 21×5 tiles（336×80）金框黑底信息提示窗口（军师头像 64×64 + 自动换行文字「請指示下個月以後的財政予定。」）。
  - 屏幕绝对居中对齐，提示框与财政主窗垂直紧凑排列；
  - 左上：`資金` 白字 + 黑底数值框，实时显示势力金钱（`16870` 等）；
  - 右上：`收入` / `支出`，中间白色细竖线分隔，右侧黑底数值框显示实时预估月收入与预估月支出（预备兵维护费 + 武将俸禄）；
  - 左下：`今月底` 白字居中，包含红底天平图标与当前税率（`17%`），3 行红底兵种图标（骑/弓/步）与本月已生效征兵数；
  - 右下：`次月` 白字居中，包含绿底天平按钮与下月税率设定，3 行绿底兵种按钮与下月各兵种征兵数设定；
- **数字字体细体化**：全局纯数字统一采用 `300 16px "Oswald"`（`Oswald-Light.woff2`），字形纤细典雅不笨重；
- **全菜单统一信息提示窗口规范**：
  - 构建全系统统一规格的信息提示窗口：黑底金框，高度固定 5 tiles = 80px（内高 64px），宽度统一取武将弹窗宽度 480px（30 tiles）；
  - 固定显示在屏幕底对齐、水平居中，并与底部保持 8px 间距，不紧贴屏幕边缘；
  - 头像统一使用 `web/grf/ui/message_npc.png`（64×64）；文字统一为白色并自动按可用宽度折行；
  - 覆盖范围：武将、势力、据点（首都确认/据点一览）、编成、财政等一级菜单弹窗的提示窗口全部统一；弹窗内的信息提示（如特长对白、编成成功/失败发言）保持原样；
- **右侧君主/资源面板 UI 细节精细还原（对照原版像素级对齐）**：
  - 君主/首都/军师标签（x=76）与右侧姓名（x=138）之间增加 1px 白色垂直分隔线（x=122.5，纵向从 y+14 至 y+61）；
  - 信赖度进度条变细（红条高度精调为 2px，黑底凹槽 8px）。
- **数字键盘弹窗加大与精确定位 (`keypadDialog`)**：10×7 tiles（160×112，内部 144×96）黑底金框浮动弹窗。
  - **位置精确定位**：位于当前设置对象数字下方偏右（`ox = fx + 252`），离开图标列有 6px 间距，不遮挡当前及以上行数字（税率、骑兵、弓兵、步兵分层顺延）；
  - **按钮增大**：数字/功能键扩大为 24×24px，右侧操作键为 48×24px，按键舒适易点击；
  - 顶部 144×24 黑底数字显示区，细体居中实时显示输入值；
  - 4×3 绿底数字/功能键（`7 8 9`、`4 5 6`、`1 2 3`、`0`、`00`、`◀` 回退键）；
  - 右侧 2×3 橙金底操作按钮（`取消`、`最大`、`決定`）；
  - **取消键功能**：点击「取消」将当前输入的数值复位清零为 0（不关闭窗口）；
  - 严格上限钳制：税率最大 100，各兵种征兵最大 10000；输入超限或点击「最大」自动钳制为对应上限；
  - 回退机制：◀ 从末尾取消一位；決定保存并关闭弹窗；
- **真实经济与换月结算动力学 (KI.EXE 0x5358 / 0x53C6 / 0x54FC / 0x5538 / 0x5547 / 0x5695 逆向实锤)**：
  - 切比雪夫距离衰减（`distLevel`）：城池距首都距离 $\le 80 \to 2$、$\le 200 \to 3$、$> 200 \to 4$；
  - 据点原始税收基数：$\sum (\text{prod} / L)$，玩家收入 $= \text{基数} \times \text{税率}\%$；
  - 据点征兵产出基数：$(\text{prod} / L) / 32$，并根据南北地理纬度（$y < 80$ 北方多骑兵、$80 \le y < 150$ 中原多步兵、$y \ge 150$ 南方多弓步兵）计算各兵种产出上限；
  - 设定征兵数受据点产出上限钳制后并入预备兵池；
  - 据点生产力与上升率动力学（0x5695）：玩家税率与 30% 基准比较（$<30\%$ 正增长，$>30\%$ 衰退）；
  - 城兵与防灾月度恢复（0x4194）：太守/内政官政治力提供加成；
  - 换月时自动将次月设定复制覆盖至当月，并刷新势力金钱、预备兵与据点属性。

---

## 三、此前轮次已完成的稳定功能（勿返工）

- **大地图交互**：据点信息卡（含类型右对齐）、據點/軍團选择菜单、行军军团信息列表；旧命令面板不再被地图点击触发。
- **势力颜色**：`world.js FACTION_COLORS` 20 色（不含红/白/灰，红留玩家 `#e02020`），`factionColorEx`、`contrastText`（YIQ≥128 黑字）。
- **据点类型（二进制实锤）**：城池记录 **+0x16 低半字节**，字符串表 VA 0x7DF5：0 大都市/1 中都市/2 小都市/3 關卡/4 戰場/5 港都。
- **智能寻径（用户三规则全落地）**：`pathfind.js`+`ai.js`。路网从地图 tile 奶油线像素提取（43 线 tile+桥180+城关205/208/211+水95/202+245 坐标级补丁格）→ `road_cost.bin`（384×256，192/192 城全陆路连通）；`road_offset.json` 线芯偏移。规则：①A* 最短不下道（陆1/水3/出入水+40）②中立/敌对不挡路、到达即攻、战败退 `_bases` 最近据点休整 12 天 ③第三方堵死→佯动回城。城门只走正方向；9 关卡均南北向（`gateDirs`）。原版依据：KI.EXE 0x491B 图 Dijkstra（1024 节点）、0x48FD 势力检查。
- **行军「凸」标识**：`grf/ui/mark0-3.svg`（`__FC__` 染势力色，势力%4），`mapview.js` 旋转朝行进方向。**用户可替换 SVG**。
- **外交**：关系≤0x80=交戰（红色）；新军团 6 天整备期（cooldown，原版机制）。
- **小地图（已验收）**：窗口 14×11 瓦片 → 外框 224×176、内区 208×160；地图 208×139 置顶 + 底部双名牌；固定右上角；据点 5×5 像素点；右名牌弹出「勢力一覽」；遇袭闪烁 3 次（1500ms）。

---

## 四、失败尝试与设计禁区（勿重复）

1. **小地图底纹找精灵表**：ICONGRF/IVENTGRF/KYOGRF 三表逐一扫描无任何棋盘格 tile → 底纹是程序化逐像素抖动（VGA 16 色时代标准技法），**不是贴图**。
2. **从用户截图提取逐像素图案**：截图被缩放（340×222 vs 地图 384×256），(x+y) 奇偶相关性≈0.5 → 只能取色系，无法还原精确几何。
3. **固定灰褐 `#C5BFA1` 做棋盘第二色**：会把用户手工渐变的深色区"洗白" → 用户定稿：陆地直接用底图像素，不抖动。
4. 凸标/势力颜色的原版静态表均在运行期数据段（0x987A 与地图装载区重叠），静态扫不出——SVG/20 色表方案已定稿，勿再挖。
5. 道路 tile 纯阈值分类必然失败（平原黄花散点混入）；修复循环必须纯陆路（水代价 99，否则许昌→洛阳绕海 554 步）。
6. `load_tiles()` 返回 list 不是 dict。
7. **全游戏绝不允许添加任何关闭按钮**，所有弹窗与二级菜单的关闭与回退一律使用鼠标右键。

---

## 五、相关文件索引

| 文件 | 内容 |
| --- | --- |
| `web/src/ui/gamebar.js` | 工具栏/小地图/资源面板/名牌/势力列表/编成面板/遇袭闪烁/计时冻结(syncClock) |
| `web/src/ui/hud.js` | 模态弹窗 `_modal`、武将列表、编成选将、据点一览、進言（采纳/驳回/撤回） |
| `web/src/main.js` | `frame()` 60fps逐帧渲染主循环（syncClock→clock.advance→日期变化重绘）、loadState |
| `web/src/game/clock.js` | 时钟（速度档/hold/dayProgress 逐帧进度） |
| `web/src/game/talk.js` | TALK.DAT 逆向对白解析（特长台词、编成发言、进言话术） |
| `web/src/game/{pathfind,ai,diplomacy,world,commands}.js` | 寻径/AI/外交/势力色/命令系统/月度结算 |
| `web/src/render/mapview.js` | 大地图渲染、逐帧插值、凸标平滑移动、道路中线贴合、行军虚线、驻军牌 |
| `web-port/tools/compose_minimap.py` | 合成最终 minimap_roads.png（用户底图+河流+道路+海洋棋盘） |
| `web-port/docs/re-notes-kernel.md` | 557 行 KI.EXE 逆向实锤笔记 |

---

## 六、当前状态与下一步

**状态**：

- 军师菜单「財政」弹窗、数字键盘输入系统（税率上限 100、征兵上限 10000）及换月结算经济系统已完整闭环并 1:1 像素级还原。
- 势力信息面板（右侧君主/资源面板）资金、预备兵（骑/弓/步）图标与排版已 1:1 还原原版黑底红标规格，动态绑定势力资源。
- 军师菜单「編成」功能及发言/警告弹窗（加宽 30% 及排版优化）已完整就绪。
- 本地开发服务器 8321 正常运行中。

**下一步（按优先级）**：

1. **人事系统模块**：
   - 军师菜单「人事」子界面（武将登用、赏赐、任命、俘虏处置等）。
2. **军团管理与外交 AI 扩展**。
