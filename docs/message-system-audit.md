# 原版消息系统覆盖审计

> 审计日期：2026-09-03。分类按**因果来源**，不是按说话人：玩家操作直接引发为主动；AI、月结、自动战斗及灾害引发为被动。结论分为实锤、实现偏差与待证，不把Web自拟HUD文案视作原版消息。

## 1. 原版执行模型（实锤）

原版消息分两层：

1. **同步阻塞消息链**：战前报告、战后武将去向、失城/迁都/灭亡、玩家进言等在规则函数中调用 `0x8810`，返回前主流程不继续。
2. **四页战略事件轮**：256个4B槽、4页×64槽；新局/月结首槽等待7次势力调度，之后每10次消费一槽。type6/type7的20是事件槽偏移，不是天数；出队时才重读外交官并计算结果。

Web统一呈现契约：FIFO；显示期间 `clock.hold`；普通NPC/武将框3秒自动关闭或右键关闭；回调once；战术层及高优先级模态期间延后；状态必须在原版规定的对白关闭边界提交。外交费type5是强制交互例外：整段屏蔽右键，只允许左键选择、输入和推进；右键不能退出、暗中当作拒绝或改写已批准预算。

## 2. 主动消息矩阵

| 消息链 | 原版证据 | Web状态 |
| --- | --- | --- |
| 敌对进言与玩家正式宣战 | `0x6405→0x6475→0x3830→0x645D→0x3526`；TALK86..149、486..488 | 已实现；宣战在君主命令关闭后提交。真实FIFO生命周期此前无focused test，本轮补测试。 |
| 停战提案/使者返回 | `0x64F1`；type6；TALK73、57、43..46 | 事件轮已实现。审计发现成功状态在结果框前提前提交、结果框3.5秒；本轮改为结果框3秒关闭时提交。 |
| 请求协助/使者返回 | `0x6623`；type7；TALK74、57、47..50 | 同上，本轮修正关闭边界与3秒。 |
| 玩家迁都 | `0x6909→0x33FD`；TALK61、选择器0x182/0x1A4 | 已补真实进言生命周期测试；修复此前最终君主对白显示前就提交首都与信赖的问题，现为3秒/右键关闭后once提交。 |
| 我方发起攻城/道路遇敌 | `0x4ED7→0x4F58` TALK28；`0x4EB9` TALK29 | 本轮已接入权威FIFO；TALK28/29关闭前不打开战术层，右键与3秒timer竞争只启动一次。委任/无真实守军速算保持静默。 |
| 战后逃脱、被俘、回归 | `0x291A→0x2977/0x29C3→0x2A7E`；TALK31..35、67及个性选择器 | TALK31..35、67已进入权威FIFO；被俘0x19A和48槽回归0x198现按`0x075B`公式展开为紧邻第二段，第一段关闭后才显示。 |
| 玩家攻灭势力后的逐将链 | `0x4CF3→0x4D63→0x4DF0→0x4DA4→0x4FCE`；TALK68/69/31..37/67 | 内政官TALK68→0x1A6、外交官TALK69→0x1A7、逐将TALK34→0x19A、TALK37→0x199及最终TALK36均已按原顺序进入FIFO。 |

## 3. 被动消息矩阵

| 消息链 | 原版证据 | Web状态 |
| --- | --- | --- |
| AI向玩家宣战 | type1 `0x320C→0x3526`；TALK63→478..480 | 已全部改为真实TALK数据驱动；TALK63→个性478..480严格FIFO，第二段关闭才提交战争状态，覆盖3秒/右键once与持续hold。 |
| 玩家势力被系统自动选为宣战发起者 | `0x2BD9→0x2EFB→type1`；TALK486..488 | 已实现，正确归类为系统被动事件。 |
| 敌军攻击/玩家据点失陷/首都失陷 | TALK27、26、30 | 本轮已接入：有真实非委任守军时TALK27关闭后开战术层；玩家据点实际易主统一TALK26；首都失陷且找到替代首都时按TALK30→TALK26顺序入FIFO。 |
| 边城请求援军 | `0x3EFD→0x40C9` TALK38 | 已改为TALK38数据驱动并保留24..39槽冷却；玩家无守军边城触发，AI城静默。 |
| 势力灭亡全局公告 | `0x4FCE`末尾 TALK36 | 已改为TALK36数据驱动，并在内政官、外交官、逐将消息之后进入同一FIFO。 |
| 俘虏逃亡/归降/原属回归 | 月结 `0x585F→0x5940`；TALK65/66；type9 TALK37 | 已改为TALK65/66/37数据驱动；TALK37后的0x199个性对白已按`0x075B`选择器接入。 |
| 新武将投奔 | TALK41 | 本轮已改为玩家势力TALK41进入权威FIFO；AI势力投奔静默，并新增独立测试。 |
| 内政/外交预算 | type4/type5；TALK56/TALK57及结果池 | audience与生产器已实现；type5公开输入分发保证选择、金额及结果阶段均屏蔽右键，只允许左键完成流程，且不会二次清零预算。 |
| 入站协同/停战请求 | type2/type3；TALK360..384 | audience已实现。审计发现结果显示前提交；本轮改为最终结果框关闭时once提交。 |
| AI迁都 | type8 `0x33EA`；TALK57→个性选择器0x1A4；无外交官静默 | TALK57和按外交官`talk_idx`选择的0x1A4第二段均已接入；无外交官保持静默。 |
| 暴风雨/大火/暴动 | type11 TALK70；type12 TALK71/72 | 本轮已按TALK70/71/72拆分并接入统一TALK/FIFO，补齐arg0=1/2索引测试。 |
| 严重赤字训斥 | type13 `0x3507`；TALK51→`0x3DC9(AL=50,CX=0x196)` | 审计发现入队前扣信赖；本轮改为消息关闭后扣50并检查Game Over。 |
| type10通用存档TALK | `0x3496`，仅DOS SAVE事件轮已有条目 | 消费器和真实FIFO测试已完成，覆盖SAVE槽出队、通用占位替换、3秒/clock hold；仍不得给新游戏自创生产器。 |

## 4. 本轮已修复的消息基础设施

- 新增 `tools/verify_strategic_message_fifo.mjs`，覆盖NPC→武将→NPC严格FIFO、3秒计时、右键与旧timer竞争的once、全链clock hold、模态门控恢复、reset期间异步头像旧消息不复活、入站外交最终关闭提交。
- 新增 `tools/verify_negotiation_message_commit.mjs`，验证玩家停战/请援使者的外交、目标与资金状态只在3秒结果框关闭后提交。
- 普通消息自动关闭timer绑定具体卡片，旧timer不能关闭后继消息。
- `_drainStrategicMessages()`增加场景UI generation守卫，返回标题/reset期间等待头像或TALK的旧消息不能复活并提交旧状态。
- `message.onClose`及入站外交commit使用`try/finally`收尾，回调异常不再永久卡住FIFO/clock hold。
- `onModalClosed()`现在主动恢复消息drain。
- 新增 `enqueueTalkMessage()`：有实锤TALK索引的规则事件统一通过TALK格式化后进入同一FIFO；已迁移TALK26..37、41、57、65..72。
- 已移除野战/攻城结果的自拟HUD摘要；战果对玩家的正式反馈只走原版TALK26..35/67及其个性对白，AI/委任速算无原版消息的路径保持静默。
- 复刻 `0x075B` 的选择器换算：`0x196 + (selector-0x196)*8 + general[+0x1E]`；通用段与个性段作为相邻FIFO条目，严格在第一段关闭后推进。
- 新增 `verify_extinction_message_fifo.mjs`，走真实`applyBattleResult→GameBar`，验证TALK68→0x1A6、TALK69→0x1A7、TALK34→0x19A、TALK36全链顺序、逐段3秒与持续clock hold。
- 复核`0x50B4`：仅调用`0x2BA8`清军团地图状态、`0x7028`注销军团并清武将`+0x17/+0x1C`，没有直接或间接TALK调用。`0x4D63`的TALK68条件仅为旧城非中立且`city[+0x19]!=FF`，函数内无玩家阵营判断。
- `verify_budget_message_ui.mjs`：type4/type5从真实GameBar出队；现经公开`GameBar.click()`覆盖type5选择、金额及结果阶段右键均无效，左键可完成“提示金额”与批准提交，预算不被二次清零且后继FIFO门控正确。旧测试只调用私有`_clickProposalAudience()`，曾漏过公开右键分发把已批准预算重写为0的缺陷。
- 新增 `verify_type10_message_fifo.mjs`：从真实战略事件槽出队type10，验证通用占位格式化和3秒clock-hold生命周期。
- 新增 `verify_advice_commit_boundary.mjs`：实测玩家迁都与请求君主出阵的最终对白关闭边界；修复两条成功路径此前在最终君主对白出现时即提交状态的问题，并让结果阶段右键等价于3秒完成回调且once。

## 5. 审计收尾结论

- 战略战果HUD旁路已清除；TALK26..38、41、51、56/57、63、65..72及宣战个性窗口均进入权威FIFO。
- 玩家迁都成功/失败、君主出阵成功/失败/已出阵TALK64，以及设置/战术模态期间被动消息等待均有边界测试。
- AI对AI攻灭势力的动态测试确认`0x4D63/0x5074`无玩家门控，仍产生TALK68/69同步链。
- 全量扫描`0x8810`调用点与`CX>=0x196`立即数后，本次涉及的战略生产者已闭合；0x19B/19C/19D/1A2/1A3等属于其它人事/内政菜单域，不冒充本次战略事件生产者。
- Web即时反馈仅保留存档结果、无效操作和开发中的非规则提示；不再承担原版战略规则消息。
