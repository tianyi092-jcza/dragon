# AGENTS.md — AI 协作指南

卧龙传（1995 DOS 三国策略）→ Web 复刻。**纯原生 JS (ES Modules) + Canvas 2D，无框架、无构建、无 npm 依赖。**

## 运行与验证

```bash
python tools/webserver.py 8321   # 静态服务（3000 常被占用，用 8321）
node --check x.mjs               # 语法验证：先 cp file.js /tmp/x.mjs（--check 需 .mjs 后缀）
playwright-cli open http://localhost:8321   # 浏览器冒烟测试；截图/临时脚本用完即删
```

- LSP/TS 诊断在无 tsconfig 下是推断模式，偶发伪影（如 startmenu.js L744 EOF 误报）——以 `node --check` 为准
- hud.js 有 innerHTML 的 ast-grep ignore 注释，格式是 `pi-lens-ignore: no-inner-html-js`（勿改坏）

## 目录

| 路径 | 内容 |
| --- | --- |
| `web/src/` | 游戏源码：`game/`（世界/经济/AI/寻径/战斗）、`ui/`（开始菜单/游戏栏/HUD）、`render/`（地图/战斗渲染）、`core/`（输入/时钟/资源） |
| `tools/` | Python 解析器：原版 DAT → `web/data.json` 等资产 |
| `docs/` | 逆向笔记（re-notes-kernel.md 等），深入细节先查这里 |
| `web/grf/` | 提取的图像资产（ui/kao/kyo/四季地图） |

## 数据管线（改数据必走）

1. 游戏真数据在 **`E:/Dragon/Dragon/SINARIO.DAT`**（运行时数据）；`E:/Dragon/{上,中,下,后,原版}/SINARIO.DAT` 是剧本库
2. `web/data.json` = 五目录合并的 **20 章合集**（上=1~4章、中=5~8、下=9~12、后=13~16、原版 4 章加 `[原版]` 前缀），由 `tools/parse_sinario.py` 生成——**改了解析器必须重跑**，输出到 `web/data.json`
3. 势力军师名来自势力记录 byte[2]（0x7F=无→显示－－－），不是政治最高武将
4. 势力记录字段：资金=24bit（word@0x20 + 高位@0x22，何進 8464+1×65536=74000）；预备兵三池 騎/弓/步 = bytes 4/6/8 的 u16，**存储单位=十人**（原版显示×10，劉備 存100/200/400 顯示1000/2000/4000）；`f.troops` 总池=三池之和×10

## 坐标系（易错）

- 游戏逻辑分辨率 **640×400**，主画布 `#cv`（CSS 缩放）；**开始菜单用独立画布 `#startv`**（页面偏移约 left=80, top=100）——playwright 点击换算：`screen = game + (80,100)`
- 城池/军团坐标 = `mmap_map.bin` 地形网格 **384×256**（每格 16×16px）；水域 = 图块索引 95(海)/202(江)，寻路绕行
- 弹窗居中用 `prompt({x:"center",y:"center"})` 动态计算，不写死坐标

## 关键逆向机制还原 (2026-08 最新定稿)

- **内政官治理贡献与治理计划费用 (KI.EXE 0x4194 / 0x5715 - 0x576B)**：
  - **治理贡献动力学 (0x4194)**：
    - `cl = 5 + (有内政官 ? 政治力 : 0)`，`dl = (1 + (有内政官 ? 统率力 : 0)) >> 1`，步长 `ch = Math.max(1, cl - 15)`；
    - 每日判定：当 `cl >= rand(16)` 时，据点上升率/士气增加 `ch`（上限 200）；防灾恢复增加 `(ch >> 1) + 1`（上限 200）；
    - 城兵恢复：城兵离上限有差距时，以每日概率向城兵填充 `dl`（统率加成）；
  - **内政官每月治理预算 (0x5715 - 0x576B)**：
    - 针对每个任命了内政官的据点，计算其上升率、防灾、城兵离上限的差距之和 `totalGap = (gapGrowth + gapDisaster + gapTroops) >> 1`；
    - 据点治理费计划支出为 `totalGap * 50` 金，直接计入势力每月预估支出中并在换月时结算扣除。
- **外交官常态维护与外交预算 (KI.EXE 0x3E8E / 0x578F - 0x57E1)**：
  - **常态友好度维护 (0x3E8E / 0x30D3)**：
    - 派驻外交官的势力，每月根据外交官政治属性计算关系改善增量 `gain = Math.max(1, Math.floor(politics / 4))`；
    - 双方友好度向上限 100（`0x64`）逐渐改善，若非交战状态则持续稳定维持亲密/良好关系；
  - **外交官每月外交费预算 (0x578F - 0x57E1)**：
    - 针对派驻外交官的目标势力，按双方友好度差距 `relGap = Math.max(0, 100 - minRelation)` 计算；
    - 该势力的每月外交预算为 `relGap * 200` 金，计入势力每月财政预估支出。

## 术语与菜单约定（统一规范，不再赘述）

- **军师菜单与一级工具栏交互规范（2026 最新定稿）**：
  1. **一级菜单乒乓开关与最高优先级**：
     - 顶栏羽扇图标（`fan`）是一级菜单的乒乓开关，点击在“打开菜单”与“隐藏菜单”之间切换；
     - **优先级最高**：当再次点击羽扇隐藏军师菜单时，立即强制关闭所有子孙窗口、二级下拉菜单、信息卡片及列表弹窗，取消所有选中状态（`selectedSubmenu = null`, `selectedCity = null`），并恢复计时；
     - **唯一关闭途径**：能关闭军师菜单条的**只有其父菜单项（羽扇图标）的开关切换**，鼠标在地图任何位置左键或右键均不关闭军师菜单条本身。
  2. **军师子菜单初始状态与无悬停效果**：
     - 每次打开军师菜单时，所有 8 个子菜单项（進言/人事/財政/編成/軍團/據點/武將/勢力）均初始化为**未被选中状态**；
     - 菜单项无鼠标悬停半透明底色效果，左键点击即进入选中高亮状态（实心米黄底黑字）。
  3. **单选互斥与全局地图绝对锁定**：
     - 当军师子菜单任何一个项被点击选中打开时，**整个游戏大地图完全锁定不可拖拽平移，地图上的任何操作与悬停拾取全部无效**；
     - 只有通过**鼠标右键**取消关闭子界面、返回到军师菜单“8 项全未选中”状态时，大地图平移与交互才恢复正常。
  4. **大地图左键交互纯粹性**：
     - 在大地图上，**除左键点击据点中心或行走中的军团标识外，左键点击地图任何其它位置（空白区、山川、河流、空地）均没有任何功能**，不关闭二级菜单、不关闭弹窗、不取消选中据点。
  5. **严格右键关闭与层级回退**：
     - 全游戏无关闭按钮。点击据点中心展开的弹窗（据点信息卡片、驻军据点 7×3「據點/軍團」选择菜单、军团情报弹窗）以及军师二级菜单，**必须且只能通过鼠标右键关闭**。
     - 右键层级回退：
       - 第 1 级（若有子弹窗或选中据点）：关闭最内层弹窗/卡片/二级菜单，清除据点中心正方形选中框，取消军师子菜单选中状态，恢复未选中并立即开始计时、解除地图锁定；军师菜单条保持展开；
       - 若军师菜单已处于未选中状态，再次右键不关闭军师菜单（军师菜单只能点击羽扇图标关闭）。
  - **弹窗与列表样式规范**：
    1. **滚动条统一位于右侧（`scrollbar: "right"`）**：全游戏所有 Canvas 列表弹窗（据点一览、武将列表、势力列表、军团情报等）滚动条一律统一靠右侧排列，右侧预留 18px 滚动通道与上下箭头，保持界面视觉风格完全统一。
    2. **据点与驻军子菜单**：
       - **展开据点二级菜单**：点击「據點」后展开 2 项下拉弹窗（`首都確認` / `據點一覽`），宽度 7 tiles，黑底金框云纹；
       - **首都確認**：点击「首都確認」：关闭下拉菜单，「據點」保持选中高亮状态，大地图平滑移动并居中至我方首都据点，左下角弹出首都信息卡片，首都中心显示正方形选中框；右键关闭卡片并恢复未选中状态、清除正方形选中框、开始计时；
       - **據點一覽**：点击「據點一覽」：关闭下拉菜单，弹出我方据点一览弹窗（表头：據點名、生產力、上昇率、防災、城兵、內政官；底部提示框：*將游標移動至指示之據點。*）；
       - **据点列表中点击某一行据点**：关闭列表，大地图中心移动至该据点，左下角显示该据点信息卡片，据点中心显示正方形选中框，「據點」菜单保持选中高亮；右键关闭卡片，清除选中框，恢复军师菜单全部未选中状态并开始计时；
       - **在地图上直接点击据点**：
         - 若军师菜单已展开，在地图上点击据点中心时**不关闭军师菜单**；
         - 点击中立或无军团驻守的据点后，据点中心显示正方形边框表示当前选中，左下角弹出据点信息面板；右键关闭信息弹窗时该正方形框消失，取消选中状态；
         - 点击有驻军的据点时，弹出 7×3 tiles 黑底金框垂直子菜单（`據點` / `軍團`，悬停黄底蓝字），据点中心显示正方形选中框；点击「據點」显示据点信息卡片，点击「軍團」显示军团情报弹窗；
       - **軍團情報弹窗规范**：
         - 弹窗表头与列：`武將名`、`總兵數`、`士氣值`、`現在位置`、`目標據點`（统一右侧 18px 滚动条，共 10 行，不足 10 行以虚线 `－－－－` 占位）；
         - 当该据点驻守有多个军团时，在列表中按顺序显示多个军团的信息；
         - 右键关闭军团情报弹窗，清除据点正方形选中框，并恢复计时。
    3. **「人事」子菜单（內政官任命/解任、外交官任命/解任）**：
       - **展开人事二级菜单**：点击「人事」后在下方弹出 4 项下拉弹窗（`內政官任命` / `內政官解任` / `外交官任命` / `外交官解任`），规格 8×6 tiles，黑底金框，左对齐人事按钮偏左 22px 位置；
       - **3 秒自动关闭机制**：人事系统中所有子提示弹窗（NPC 赴任提示/未派遣提示、武将对白弹窗「我立刻前往。」/「那我這就回京城。」/「遵命。」）均支持 **3 秒后自动关闭或鼠标右键立即关闭**，关闭后立即执行后续回调并刷新对应列表；
       - **內政官任命**：
         - 点击「內政官任命」关闭下拉菜单，「人事」保持选中高亮状态，大地图绝对锁定并停止计时，在屏幕中央弹出我方所有据点一览弹窗（480×352，表头：`據點名`、`生產力`、`上昇率`、`防災`、`城兵`、`內政官`；已任命内政官的据点显示内政官名，未任命则显示 `－－－`；底部统一提示框：*要派遣內政官到哪個據點？*，通用 NPC 头像）；
         - 若点击已有内政官的据点，该行呈墨绿色高亮（`#4a7828`），并在据点列表右侧覆盖区弹出 19×5 tiles（304×80）NPC 提示框（左侧通用 NPC 谋士/官吏头像 `message_npc.png` 64×64，右侧第一行橙黄色据点名 `{據點名}` + 白字 `已有{內政官名}　大人`，第二行白字 `前去赴任了。`）；3 秒自动关闭或右键关闭并清除行高亮；
         - 若点击未任命内政官的据点，列表平滑切换为当前所有身分为 `－－－` 的空闲武将选择弹窗（480×352，表头：`武將名`、`武術`、`統率`、`政治`、`勢力`、`身分`；底部统一提示框：*請選擇任命之武將。*）；
         - 点击选择武将后，该武将身分变更为 `內政官`（`status = 2`），据点 `governor` 绑定该武将，选中武将行呈墨绿色高亮，右下方弹出 19×5 tiles 对白弹窗（该武将 64×64 头像 + 白字对白「我立刻前往。」）；3 秒自动关闭或右键关闭，刷新返回据点列表；
       - **內政官解任**：
         - 点击「內政官解任」同样显示我方所有据点列表（底部提示框：*要解任哪個據點的內政\n官？*，通用 NPC 头像）；
         - 若点击未任命据点弹出 NPC 提示框（第一行白字`是否有所差錯？`+橙黄色据点名`{據點名}`，第二行白字`並未派遣任何人。`），3 秒自动关闭或右键关闭并清除行高亮；
         - 若点击已有内政官据点则解除其职务（`governor = null`, 武将身分恢复为 `－－－`），弹出该武将头像对白弹窗（白字`那我這就回京城。`），3 秒自动关闭或右键关闭后刷新据点列表（该据点内政官变为 `－－－`）并清除行高亮；
       - **外交官任命**：
         - 点击「外交官任命」关闭下拉菜单，「人事」保持选中高亮状态，大地图绝对锁定并停止计时，在屏幕中央弹出所有其它活跃势力列表（480×352，表头：`勢力名`、`武將`、`據點`、`首都`、`外交`、`外交官`；底部提示框：*要派遣外交官到哪個勢\n力？*，通用 NPC 头像）；
         - 若点击已有外交官的势力，该行呈墨绿色高亮，并在列表右侧覆盖区弹出 19×5 tiles（304×80）NPC 提示框（第一行橙黄色君主名`{君主名}`+白字`勢力已有{外交官名}`，第二行白字`大人前去赴任了。`）；3 秒自动关闭或右键关闭并清除行高亮；
         - 若点击未任命外交官的势力，列表平滑切换为空闲武将选择弹窗（480×352，表头：`武將名`、`武術`、`統率`、`政治`、`勢力`、`身分`；底部提示框：*請選擇任命之武將。*）；
         - 点击选择武将后，该武将身分变更为 `外交官`（`status = 3`），势力绑定外交官（`sc.envoys[faction.idx] = { name: gen.name, gen_idx: gen.idx, left: 6 }`），选中武将行呈墨绿色高亮，右侧弹出对白弹窗（该武将 64×64 头像 + 白字对白「遵命。」）；3 秒自动关闭或右键关闭，刷新返回势力列表；
       - **外交官解任**：
         - 点击「外交官解任」关闭下拉菜单，「人事」保持选中高亮状态，大地图绝对锁定并停止计时，在屏幕中央弹出所有其它活跃势力列表（480×352，表头：`勢力名`、`武將`、`據點`、`首都`、`外交`、`外交官`；底部提示框：*要解任哪個勢力的外交官？*，通用 NPC 头像）；
         - 若点击未派遣外交官的势力，该行呈墨绿色高亮，并在列表右侧覆盖区弹出 19×5 tiles（304×80）NPC 提示框（第一行橙黄色君主名`{君主名}`+白字`勢力仍未派遣任`，第二行白字`何人．．．`）；3 秒自动关闭或右键关闭并清除行高亮；
         - 若点击已有外交官的势力，解除其外交官职务（`sc.envoys[faction.idx]` 删除，武将身分恢复为 `－－－`），该行呈墨绿色高亮，右侧弹出该武将头像对白弹窗（该武将 64×64 头像 + 白字对白「遵命。」）；3 秒自动关闭或右键关闭，刷新返回势力列表（该势力外交官变为 `－－－`）并清除行高亮；
       - **多级回退规范**：子弹窗（NPC 提示框/武将对白弹窗）内右键立即关闭并执行后续回调；武将选择列表中右键返回上一级列表；第一级列表中右键关闭列表并恢复未选中/开始计时。
    4. **「武將」子菜单**：
       - 武将列表子弹窗支持进一步点击武将展开**武将信息与特长对白弹窗**（16×5 tiles = 256×80，黑底金框，左侧头像 64×64，右侧台词），选中的武将行呈墨绿色高亮（`#4a7828`）；
       - 多级右键回退：第 1 次右键关闭武将信息弹窗，第 2 次右键关闭武将列表并恢复未选中、开始计时，第 3 次右键关闭军师菜单条。
    5. **「編成」子菜单与部队编成面板**：
       - **入口与武将选择**：点击「編成」锁定地图并停止计时，弹出武将选择列表（480×352，表头：`武將名`、`武術`、`統率`、`政治`、`勢力`、`身分`），仅列出身分为“－－－”的空闲在野/在职武将；底部提示框：*進行軍隊編組。\n請選擇武將。*；
       - **编成弹窗规格（15×12 tiles = 240×192，inner 224×176）**：
         - 位于列表右侧覆盖区，选中的武将行呈墨绿色高亮（`#4a7828`）；
         - 底部提示框文字更新为：*請下達各部隊編成之指示。*；
         - 顶部：左侧 64×64 武将头像，右侧显示`將軍 {武將名}`、`總兵力 {实时总兵力}`（白色）、`士氣值 200`（上限）；
         - 左下方 6 队编制：`主將`、`前鋒`（默认騎兵）、`左翼`、`右翼`（默认步兵）、`左備`、`右備`（默认弓兵）；
         - 兵种按钮：22×14 绿底矩形（`#509040`）内嵌黑色兵种剪影图标；点击按`騎兵 → 步兵 → 弓兵 → 騎兵`循环切换；
         - 右下方预备兵：`預備兵數`标题，3 行预备兵（22×14 红底图标 `#d00000` + 剩余兵力数值，按騎兵、弓兵、步兵顺序排列）；
         - 底部`確定`按钮：金黄底黑字 3D 浮雕按钮；
       - **兵力分配与守恒算法**：
         - 6 队每队上限 1000 人；若某兵种拥有的总预备兵池充足（$\ge k \times 1000$），则该兵种各队均分配 1000，剩余保留在预备兵池；
         - 若该兵种总兵力不足（$< k \times 1000$），则将总兵力平均分配给该兵种的所有队伍（前余数队多分 1 兵），预备兵池清零；
         - 每次切换兵种按钮时，自动重新计算各队兵力与剩余预备兵，面板上的`總兵力`实时合计变动；
       - **确认与回退**：
         - 点击`確定`：扣除势力对应预备兵，武将身分变为`軍團長`（`status=1`），在势力首都生成新军团并驻守，镜头居中至首都并选中首都，提示*「{武將} 軍團編成完畢，駐守於 {首都}。」*，关闭弹窗并恢复计时；
         - 弹窗内按右键取消编成：关闭编成面板，清除列表行选中高亮，底部提示恢复为*進行軍隊編組。\n請選擇武將。*；列表内再次右键关闭列表并恢复未选中/开始计时。
    6. **「進言」子菜单与「敵對提案」进言对话系统**：
       - **展开进言二级菜单**：点击「進言」展开 8×7 tiles 下拉菜单（`敵對提案` / `停戰提案` / `請求協助` / `遷都` / `請求君主出陣`），黑底金框云纹，悬停黄底蓝字；
       - **交战势力选择弹窗**：点击「敵對提案」关闭下拉菜单，「進言」保持选中高亮状态，锁定大地图并停止计时，弹出所有其它活跃势力列表（480×352，表头：`勢力名`、`武將`、`據點`、`首都`、`外交`、`外交官`；底部通用 NPC 提示框：*請選擇交戰之勢力。*）；右键关闭列表并恢复未选中/恢复计时；
       - **进言对话事件窗口（KI.EXE 0x6475, 0x3830 逆向复刻）**：
         - 点击目标势力后进入对话窗口，背景为金框云纹事件图（`ivent_0_a.png` 288×352，外框 20×24 tiles 居中）；
         - 上方为君主发言提示框（17×5 tiles，左侧君主头像 64×64，右侧对白），初始显示君主问候（Talk 86..88）；
         - 下方为军师发言提示框（17×5 tiles，左侧军师头像 64×64，右侧对白）；**延时 3 秒或左键点击任意区域**后出现军师进言（Talk 89：*想請主公答允對【目標君主】的進兵。*，目标君主名黄色高亮 `#ffe000`）；
         - 再次延时 3 秒或左键点击后，上方君主发言框更新为第一轮内在判定（KI.EXE 0x6475）：
           - **直接开战 (al = 1)**：对方已在进攻我方，君主同意开战（Talk 93..95），信赖度 **+20**，双方进入交战状态；
           - **已交战驳回 (al = 3)**：双方已在交战状态，君主呵斥（Talk 99..101），信赖度 **-20**；
           - **直接驳回 (al = 0)**：外交关系良好（`relation >= bellicosity * 2 + 20`），君主直接驳回（Talk 90..92），信赖度 **-20**；
           - **要求开战理由 (al = 2)**：外交关系恶化进入门槛（`relation < bellicosity * 2 + 20`），君主表态（Talk 96..98），随后在中间偏左弹出 11×7 tiles 开战理由选择菜单；
       - **开战理由选择与说服机制（KI.EXE 0x3B5A, 0x3BA9, 0x3C1E）**：
         - 5 项菜单：`外交關係惡劣` / `我國較有利` / `敵正侵攻他國` / `敵勢力疲乏` / `撤回進言`；
         - 说服所需理由数由当前信赖度确定：$\ge 224 \to 1$ 个、$\ge 144 \to 2$ 个、$\ge 32 \to 3$ 个、$< 32 \to 4$ 个；
         - 4 大理由客观判定：
           1. 外交惡劣：`relation < bellicosity + 15`
           2. 我國有利：`my_cities * (bellicosity + 20) > enemy_cities * 25`
           3. 敵侵他國：目标势力攻击目标非空且非我方
           4. 敵勢疲乏：目标势力资金为负/赤字
         - 点击理由后下方军师发言（Talk 103..107），延时/点击后君主回应：
           - **撤回進言**：君主 Talk 144..146，**不扣信赖度**，3 秒或点击后退出对话；
           - **重复理由**：君主 Talk 147..149，不扣信赖度，3 秒或点击后重新弹出理由菜单；
           - **假理由**：君主训斥（Talk 108..110 / 117..119 / 126..128 / 135..137），**信赖度 -20**，进言失败并退出；
           - **真理由（仍需补充）**：君主 Talk 114..116 / 123..125 / 132..134 / 141..143，3 秒或点击后重新弹出理由菜单；
           - **真理由（说服成功）**：君主采纳（Talk 111..113 / 120..122 / 129..131 / 138..140），**信赖度 +10**，双方正式进入交战状态（关系置 0x80）；
       - **严格右键层级回退**：任何阶段按鼠标右键均可立即安全退出进言对话（不扣信赖度），关闭弹窗，清除军师菜单选中，恢复计时与地图。

## 代码约定

- **样式按前端布局思路**（居中/padding/`measureText` 右对齐/space-between），**不还原原版组件绝对定位**——用户明确要求
- 列表弹窗：游戏内画布弹窗走 `gamebar.js openListDialog()`（米黄底/黑表头/右键关闭/滚轮滚动），开局选单走 `startmenu.js prompt()`；遗留 DOM 弹窗走 `hud.js _listDialog()`
- 字体：数字列 `Oswald`（`_numText`，`Oswald-Light.woff2`），中文 `Noto Serif TC`；标题全角字 `_fwText`
- 计时机制（逆向实锲，re-notes-kernel.md「实时时钟系统」）：原版主循环 `[0xD2A]=1`（菜单开）时完全跳过时钟进位链；`[0x98A5]`=鼠标活动暂停计数器，静止自动恢复。Web 对应：`gamebar.syncClock()` 每帧断言 `clock.hold`（鼠标近 1s 活动 ∥ 子菜单/设置开 ∥ `hud.dialogCount>0`）

## 已知坑

- NPC 撤退后立即重开战（无冷却），测试时战斗层会拦截点击；`clock.speed=0` 拦不住撤退再战——可 eval 清军团或打完再测
- AI 互斗速算、仅玩家参战才开战术层（resolveBattle）——测试中战斗突然弹出是正常行为
- 旧存档 scenario_idx 与 20 章合集错位（历史遗留，未处理）

## 本轮逆向追加 (2026-08-26 晚)

- 势力记录预备兵: 仅 byte 8 = 预备兵总量(十人单位, 显示×10); bytes 4/6 待进一步逆向(原版第三章劉備显示 0/0/2000)
- 资金 = 24bit (word@0x20 + byte@0x22 << 16)
- 小地图底图 = MMAP.MAP 图块 + GAMEPAL bank2(秋季) 渲染 → tools/render_minimap.py 生成 192×128
- 小地图据点 = 3px 实心方块; 底部名牌: 左=红底白字(我方), 右=白底黑字(选中势力)
- 外交值劉備×公孫瓚=0xE4 应映射親密(≥0xDC), 若显示普通需浏览器复查
- 战斗循环修复: 玩家军团跳过 AI 决策+无军团重生跳过玩家+撤退 target=null

## 外交阈值实证 (第五章劉備开局)

- 最惡 <0x90 | 險惡 <0xB6 (呂布 0xA6/孔融 0x94) | 普通 <0xD0 (曹操 0xB6/劉闢 0xCC) | 良好 <0xE0 | 親密 ≥0xE0 (公孫瓚 0xE4)
- 顏色: 最惡=蓝 險惡=橙 普通=米白 良好=浅绿 親密=绿
- 遊戲規則: 關係降至最惡才可進言開戰(君主可能同意); 否則難以同意
